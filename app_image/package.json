{
  "name": "app_image",
  "version": "1.0.0",
  "description": "```\r #docker\r #Read Swarm.md\r #nodejs\r apt-get install nodejs\r ```\r ### Build Docker Image\r ```\r docker build -t Achronaz/stress-test .\r docker images",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
